version: '3'

volumes:
  datafiles:

services:
  db:
    image: mcr.microsoft.com/mssql/server:2017-latest-ubuntu
    volumes:
      - datafiles:/var/lib/mssql
    restart: always
    environment:
      SA_PASSWORD: Mi(r0s0ft
      ACCEPT_EULA: Y
  
  mvc:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - db
    ports:
      - "8888:80"
    restart: always
    environment:
      - DBHOST=db
      - ASPNETCORE_ENVIRONMENT=Development